plugins {
    id 'java-gradle-plugin'
}

repositories {
    mavenCentral()
}

gradlePlugin {
    plugins {
        outputDeadlock {
            id = 'outputdeadlock.plugin'
            implementationClass = 'outputdeadlock.plugin.OutputDeadlockPlugin'
        }
    }
}
